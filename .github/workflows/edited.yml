name: Testing PR Edit

on:
  pull_request:
    types:
      - opened
      - edited

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checking if this runs on edited PR.
        run: echo This runs for edited section.
  
